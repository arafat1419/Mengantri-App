<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/dp_8">

    <com.google.android.material.textview.MaterialTextView
        android:id="@android:id/title"
        style="@style/TextBoxTitle"
        android:layout_marginStart="@dimen/dp_8"
        android:maxLines="1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Language" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/preferences_value"
        style="@style/TextBoxTitle"
        android:layout_marginEnd="@dimen/dp_16"
        android:maxLines="1"
        android:textSize="12sp"
        app:layout_constraintBottom_toBottomOf="@+id/btn_preferences"
        app:layout_constraintEnd_toStartOf="@+id/btn_preferences"
        app:layout_constraintTop_toTopOf="@+id/btn_preferences"
        tools:text="English" />

    <androidx.appcompat.widget.AppCompatImageButton
        android:id="@+id/btn_preferences"
        style="@style/MiniIcon"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="@dimen/dp_8"
        android:background="?selectableItemBackgroundBorderless"
        android:src="@drawable/ic_arrow_forward"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>